# Commune Lasalle — API atelier (VPS). Versions figées depuis le venv de réf.
# Installer : python3 -m venv venv && venv/bin/pip install -r requirements.txt
fastapi==0.128.8
uvicorn==0.44.0
pydantic==2.12.5
starlette==0.52.1
slowapi==0.1.9
bcrypt==5.0.0
python-jose==3.5.0
python-dotenv==1.2.1
numpy==1.26.4
requests==2.32.5

# Collecteurs (lancés sur le Mac, hors VPS) — décommenter si la collecte tourne ici :
# beautifulsoup4==4.14.3
# pdfplumber==0.11.9
# ddgs==9.14.4
