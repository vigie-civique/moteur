# Vigie Civique — dépendances Python. Versions figées depuis le venv de référence.
# Installer : python3 -m venv venv && venv/bin/pip install -r requirements.txt
#
# Une instance amorcée depuis ce fichier doit pouvoir collecter, pas seulement
# servir l'API : les paquets de collecte étaient commentés, et un clone neuf
# échouait au premier import de BeautifulSoup.
#
# Le venv se crée DANS le dépôt de l'instance (`venv/`), jamais ailleurs. Un
# venv partagé entre plusieurs projets tient debout chez celui qui l'a fabriqué
# et nulle part ailleurs : deux collecteurs du moteur ont vécu des mois en ne
# tournant que dans le venv personnel du développeur, parce que leur paquet
# n'était déclaré ici nulle part. Tout ce que le code importe doit figurer dans
# ce fichier, ou être décrit dans « Hors pip » avec sa commande d'installation.

# ─── API de l'atelier ────────────────────────────────────────────────────────
fastapi==0.128.8
uvicorn==0.44.0
pydantic==2.12.5
starlette==0.52.1
slowapi==0.1.9
bcrypt==5.0.0
python-jose==3.5.0
python-dotenv==1.2.1
cryptography==50.0.0
# Dépôt de documents dans l'atelier : FastAPI en a besoin pour lire un
# formulaire multipart. Une saisie manuelle ne s'enregistre pas sans sa source,
# et toutes les sources ne sont pas en ligne — un PV remis sur papier n'a pas
# d'URL. Sans ce paquet, la route de dépôt répond 500 sans rien expliquer.
python-multipart==0.0.32

# ─── Collecte ────────────────────────────────────────────────────────────────
requests==2.32.5
beautifulsoup4==4.15.0
numpy==1.26.4
ddgs==9.14.4          # url_finder : recherche des sites des entités locales
certifi==2026.7.22    # archive.py : vérification SSL. Arrive avec requests, mais
                      # le code l'importe en propre — donc il se déclare en propre.
openpyxl==3.1.5       # dgf_notifications.py : les exports .xlsx du portail DGCL.
                      # Sans lui, le mode CSV marche et le mode Excel casse tard.
                      # sispea.py s'en sert aussi, en lui passant un FLUX : il
                      # refuse d'après l'EXTENSION, et l'OFB livre des `.xlsx`
                      # nommés `.xls`.
xlrd==2.0.2           # sispea.py : l'extraction 2024 de l'OFB est un classeur
                      # d'ancienne génération, que openpyxl ne sait pas lire.
                      # Les autres millésimes sont des .xlsx — le format se
                      # reconnaît aux octets, pas au nom du fichier.
ijson==3.5.1          # marches_publics.py : parcours INCRÉMENTAL des consolidés
                      # DECP. Le fichier 2019 pèse 944 Mo ; `json.loads` en
                      # demandait 5,3 Go de mémoire vive — plus que n'en a un
                      # petit serveur. En flux, le pic tombe à 23 Mo pour la même
                      # durée (backend C `yajl2_c`, livré avec la roue).

# ─── Lecture des procès-verbaux (PDF, puis OCR si le PDF est une image) ──────
pdfplumber==0.11.10
pdfminer.six==20260107
pypdfium2==5.13.0
pillow==12.3.0

# ─── Hors pip ────────────────────────────────────────────────────────────────
# bsdtar, pour les extractions SISPEA de l'OFB, publiées en archives 7z :
#   macOS le fournit ; Debian : apt install libarchive-tools
# Sans lui, le step `sispea` collecte ce que Hub'Eau publie (jusqu'à 2019)
# et le dit ; il ne casse pas.
# ocrmypdf + tesseract avec la langue française, pour les PV scannés :
#   brew install ocrmypdf   puis   tesseract --list-langs   doit lister « fra »
# Scrapling, pour les sources protégées (RNA) : chemin lu dans l'environnement,
# pas installé ici — cf. collectors/rna.py.
# Playwright, pour les pages qui n'existent qu'une fois le JavaScript exécuté
# (collectors/playwright_fetch.py). Volontairement hors de la liste : il télécharge
# un navigateur entier, et aucun collecteur livré n'en dépend. À installer sur la
# machine qui en a besoin, dans le venv de l'instance :
#   venv/bin/pip install playwright   puis   venv/bin/playwright install chromium
#
# Tests : `pip install pytest`, et rien d'autre — la suite ne doit dépendre ni des
# collecteurs ni de l'API, sans quoi un test sort sur le réseau. C'est pour cela
# qu'elle n'installe pas ce fichier. Cf. CONTRIBUTING.md et la CI.

# Extraction du fond de carte (scripts/carte_fond.py). Optionnelle : une
# instance qui garde un fond distant n'en a pas besoin, et le script le dit.
pmtiles==3.7.0
